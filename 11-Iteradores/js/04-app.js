// While se ejecuta mientras la condicin sea verdadera

let i = 1; // inicializar while

while(i < 100) { // condicion

    if (i % 2 === 0) {
        console.log(`${i} PAR`)
    }else {
        console.log(`${i} IMPAR`);
    } 

    i++; // Incremento
}