const autenticado = true;

// if - else
if(autenticado) {
    console.log('Si puedes ver netflix');
} else {
    console.log('No puedes ver netflix');
}

// operador ternario
console.log( autenticado ? 'Si est autenticado' : 'No esta autenticado');